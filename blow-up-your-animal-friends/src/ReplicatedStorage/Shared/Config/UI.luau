--!strict
-- Config-Bereich: UI/HUD (Element "UI und HUD" besitzt diese Datei).
-- PowerBar: bestehender Ladebalken. UI: vom Element zu fuellen (Timer-Anzeige,
-- Windbalken, Team-Leisten, Banner, Waffenrad-Layout).

return {
	PowerBar = {
		WidthPx = 320,
		HeightPx = 20,
		BottomMarginPx = 90,
		CornerRadiusPx = 6,
		BackgroundColor = Color3.fromRGB(20, 20, 24),
		BackgroundTransparency = 0.35,
		LowColor = Color3.fromRGB(90, 220, 90),
		HighColor = Color3.fromRGB(235, 60, 45),
	},

	UI = {},
}
