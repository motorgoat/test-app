--!strict
-- BotBrain (STUB — Element "Rundenmanagement und Match-Fluss" fuellt diese Datei).
--
-- VERTRAG:
--   takeTurn(bot, world): waehlt Ziel (naechster Gegner), rechnet mit
--   Shared/Ballistics eine brauchbare Schussloesung mit konfigurierbarer
--   Streuung (Config/Match), waehlt eine simple Waffe und feuert nach kurzer
--   Denkpause — AUSSCHLIESSLICH ueber dieselben Server-Pfade wie menschliche
--   Spieler (FireHandler/WeaponServer), keine Sonderrechte.

local BotBrain = {}

return BotBrain
